import test from'node:test';import assert from'node:assert/strict';import{csvRowsToMatches}from'../services/importService.js';test('csv rows convert to matches',()=>{const rows=[{id:'1',date:'2024-01-01',competition:'Test',homeTeam:'Brazil',awayTeam:'Mexico',homeScore:'2',awayScore:'1',venue:'neutral',xgHome:'1.5',xgAway:'0.9'}];const m=csvRowsToMatches(rows)[0];assert.equal(m.homeScore,2);assert.equal(m.xgHome,1.5);});
