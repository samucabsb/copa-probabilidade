import { normalize } from './normalize.js';const P=['u17','u-17','under17','under-17','under 17','sub17','sub-17','sub 17'];export function isUnder17Event(e={}){const raw=[e.strEvent,e.strLeague,e.strHomeTeam,e.strAwayTeam,e.competition,e.homeTeam,e.awayTeam].filter(Boolean).join(' ').toLowerCase();const n=normalize(raw);return P.some(p=>raw.includes(p)||n.includes(normalize(p)));}export const filterSeniorTeamEvents=(events=[])=>events.filter(e=>!isUnder17Event(e));
