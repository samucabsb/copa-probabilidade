import { database } from '../database/databaseAdapter.js';import { env } from '../config/env.js';export const cacheRepository={get:(k,ttl=env.cacheTtlHours)=>database.getCache(k,ttl),set:(k,v)=>database.setCache(k,v)};
