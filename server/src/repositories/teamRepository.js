import { database } from '../database/databaseAdapter.js';import { normalize } from '../utils/normalize.js';export const teamRepository={listAll:()=>database.getTeams(),findByAnyName(input){const n=normalize(input);return this.listAll().find(t=>normalize(t.name)===n||normalize(t.displayName)===n||normalize(t.code)===n||t.aliases.some(a=>normalize(a)===n));}};
