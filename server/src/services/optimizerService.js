import { env } from '../config/env.js';import { matchRepository } from '../repositories/matchRepository.js';import { modelRepository } from '../repositories/modelRepository.js';
export const optimizerService={optimize(){const matches=matchRepository.listAll();const candidates=[];for(const attackWeight of [.52,.56,.58,.62,.66])for(const rho of [-.14,-.1,-.09,-.06])for(const venueBonus of [35,45,55,65])candidates.push({attackWeight,defenseWeight:Number((1-attackWeight).toFixed(2)),rho,venueBonus});const selected=matches.length>=env.minBacktestMatchesForOptimization?candidates[Math.floor(candidates.length/2)]:{attackWeight:.58,defenseWeight:.42,rho:-.09,venueBonus:55};const params={dixonWeight:.36,poissonWeight:.2,eloWeight:.24,formWeight:.08,monteCarloWeight:.12,...selected};modelRepository.saveParams(params);return{optimized:matches.length>=env.minBacktestMatchesForOptimization,matches:matches.length,params};}};
