import { modelRepository } from '../repositories/modelRepository.js';export const calibrationService={calibrateOutcome(o){const m=modelRepository.listMetrics(5).find(x=>x.label==='backtest')?.value;if(!m?.calibration)return o;const c={...o};for(const k of ['homeWin','draw','awayWin']){const b=m.calibration[k]?.find(x=>o[k]>=x.min&&o[k]<x.max);if(b&&b.count>=5)c[k]=Math.max(.03,Math.min(.94,o[k]*.7+b.actualRate*.3));}const t=c.homeWin+c.draw+c.awayWin;return{homeWin:c.homeWin/t,draw:c.draw/t,awayWin:c.awayWin/t};}};
