import { teamRepository } from '../repositories/teamRepository.js';export const teamService={listTeams:()=>teamRepository.listAll(),getTeamOrThrow(v,label){const t=teamRepository.findByAnyName(v);if(!t){const e=new Error(`${label} inválida.`);e.statusCode=400;throw e;}return t;}};
