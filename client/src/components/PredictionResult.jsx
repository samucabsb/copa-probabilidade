import{Trophy}from'lucide-react';import{pct}from'../utils/format.js';import{StatCard}from'./StatCard.jsx';export function PredictionResult({prediction}){return <section className='mt-6 space-y-6'><div className='rounded-[2rem] bg-slate-950 p-6 text-white shadow-2xl'><div className='inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-black uppercase text-emerald-200'><Trophy className='h-3.5 w-3.5'/>Resultado mais provável</div><h2 className='mt-3 text-4xl font-black md:text-5xl'>{prediction.homeTeam.flag} {prediction.predictedScore.home}–{prediction.predictedScore.away} {prediction.awayTeam.flag}</h2><p className='mt-2 text-sm text-slate-300'>{prediction.homeTeam.displayName} x {prediction.awayTeam.displayName}</p></div><div className='grid gap-4 md:grid-cols-2 xl:grid-cols-5'><StatCard title='Gols esperados' value={`${prediction.expectedGoals.home} x ${prediction.expectedGoals.away}`} hint='lambdas ajustados'/><StatCard title='Confiança' value={`${prediction.confidence}%`} hint='dados + estabilidade'/><StatCard title='Elo dinâmico' value={`${prediction.elo.home} x ${prediction.elo.away}`} hint='força relativa'/><StatCard title='Vitória / Empate' value={`${pct(prediction.outcomes.homeWin)} / ${pct(prediction.outcomes.draw)}`} hint='lado A / empate'/><StatCard title='Vitória lado B' value={pct(prediction.outcomes.awayWin)} hint={prediction.awayTeam.displayName}/></div><div className='grid gap-6 lg:grid-cols-2'><div className='rounded-[2rem] bg-white p-5 shadow-xl'><h3 className='mb-4 text-xl font-black'>Top placares</h3>{prediction.topScores.map((s,i)=><div key={i} className='mb-3 rounded-2xl bg-slate-50 p-3'><div className='flex justify-between text-sm font-black'><span>#{i+1} · {s.home}–{s.away}</span><span>{pct(s.probability)}</span></div></div>)}</div><div className='rounded-[2rem] bg-white p-5 shadow-xl'><h3 className='mb-4 text-xl font-black'>Diagnóstico</h3><ul className='list-disc space-y-1 pl-5 text-sm text-slate-600'>{prediction.diagnostics.notes.map(n=><li key={n}>{n}</li>)}</ul><div className='mt-4 rounded-2xl bg-slate-50 p-3 text-sm font-bold'>Base local: {prediction.diagnostics.localBaseMatches} jogos</div></div></div></section>}
